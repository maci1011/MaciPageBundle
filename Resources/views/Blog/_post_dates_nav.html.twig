
<nav class="post-dates-nav navbar">
	<ul class="nav navbar-nav">

		{% if post.isPubblished and post.pubblished %}
		<li>
			<span class="navbar-text">{{ 'blog.post.published' | matra('Published') }}: {{ post.pubblished|date('d/m/Y') }}</span>
		</li>
		{% elseif post.created %}
		<li>
			<span class="navbar-text">{{ 'blog.post.created' | matra('Created') }}: {{ post.created|date('d/m/Y') }}</span>
		</li>
		{% endif %}

		{% if post.updated and (
			( post.pubblished and post.updated != post.pubblished )
			or ( post.pubblished is null and post.created and post.created != post.updated )
		) %}
		<li>
			<span class="navbar-text">{{ 'blog.post.last_update' | matra('Last Update') }}: {{ post.updated|date('d/m/Y') }}</span>
		</li>
		{% endif %}

		{% set post_permalink = url('maci_blog_post_permalink', {'id': post.id}) %}
		<li class="post-permalink">
			<span class="navbar-text">{{ 'page.permalink' | matra('Permalink') }}: 
				<a class="" href="{{ post_permalink }}">{{ post_permalink }}</a>
			</span>
		</li>

	</ul>
</nav>
