
<div class="media">


	<input name="id" type="hidden" value="{{ item.id }}" />


	{% if item.preview %}
	<div class="media-preview">
	<a href="{{ path('maci_media_get', {'path': item.preview.path}) }}" data-lightbox="media-preview" data-title="{{ item.title }}">
		{% include "@MaciPage/Media/_media_preview.html.twig" with { 'item': item.preview } %}
	</a>
	</div>
	{% endif %}


	{% if item.title %}
	<h4 class="media-title">{{ item.title }}</h4>
	{% endif %}


	{% if item.tags|length %}

	<div class="media-tags">
	<nav class="navbar-nav">
	<ul>

	{% for tag in item.tags %}

		<li><em class="media-tag">{{ tag }}</em></li>

	{% endfor %}

	</ul>
	</nav>
	</div>

	{% endif %}


</div>
