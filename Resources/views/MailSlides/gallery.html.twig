
<table class="main">
<tbody>

{% if slide.Children|length %}
<tr>

{% for child in slide.Children %}

	<td class="center">
		<img class="gallery img-fluid" src="{{ url('maci_media_get', {'path': child.media.path}) }}" alt="{{ child.media }}" />
		<span class="gallery child-title">{{ child.title }}</span>
		<p class="gallery child-content">{{ child.content }}</p>
	</td>

{% if ( loop.index % 2 ) == 0 and not loop.last %}

</tr>
<tr>

{% endif %}

{% endfor %}

</tr>
{% endif %}

</tbody>
</table>
