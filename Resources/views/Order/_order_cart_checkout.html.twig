
<form class="maci_order_checkout cart-{{ checkout }}" action="{{ path('maci_order_checkout_set_checkout', {'checkout': checkout}) }}" method="post">

	<div class="form-group">
    	{{ form_errors(form) }}
	</div>

    {{ form_rest(form) }}

</form>
