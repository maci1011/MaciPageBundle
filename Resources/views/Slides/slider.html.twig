
<div id="slide-{{ loop.index0 }}" class="slide {{ item.style }} default {% if loop.index is even %}even{% else %}odd{% endif %}">
<div class="slide-wrapper">

	{% if item.gallery.images|length %}

	{% include "@MaciPage/Media/_slider.html.twig" with { 'list': item.gallery.images, 'controls': false } %}

	{% endif %}

</div>
</div>
